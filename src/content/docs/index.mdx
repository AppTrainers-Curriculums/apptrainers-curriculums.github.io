---
title: AppTrainers Materials
description: Hands-on game-development workbooks that build real Unity games, step by step.
template: splash
hero:
  tagline: Learn to code · make games. Hands-on workbooks that take students from an empty scene to a complete, playable Unity game — one runnable step at a time.
  image:
    file: ../../assets/board.png
    alt: A wooden checkers board with pieces
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';
import { publishedMaterials } from '../../../materials.config.mjs';

## Materials

<CardGrid>
  {publishedMaterials.map((m) => (
    <LinkCard
      key={m.slug}
      title={m.protected ? `🔒 ${m.cardTitle}` : m.cardTitle}
      description={
        m.protected
          ? `${m.cardDescription} (Password required — ask your instructor.)`
          : m.cardDescription
      }
      href={`/${m.slug}/before-you-start/`}
    />
  ))}
</CardGrid>

## How the workbooks work

<CardGrid>
  <Card title="Copy-along" icon="pencil">
    Every step shows the code and explains what each part does. Type it in, press
    Play, see it work.
  </Card>
  <Card title="Always runnable" icon="rocket">
    No chapter is code you can't run. Each ends with a checkpoint you can play.
  </Card>
  <Card title="Rules first, polish last" icon="setting">
    Make the game *work* — even if pieces teleport — before making it pretty.
    Animation and sound come last.
  </Card>
  <Card title="Built for Unity" icon="star">
    Written for students who already know Unity, focused on the interesting part:
    turning game rules into clean, well-organised code.
  </Card>
</CardGrid>
