{
  "name": "materials-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "AppTrainers curriculum materials — web edition",
  "scripts": {
    "import": "node scripts/import.mjs",
    "encrypt": "node scripts/encrypt.mjs",
    "dev": "astro dev",
    "build": "npm run import && astro build && npm run encrypt",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.34.8",
    "astro": "^5.18.2",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "staticrypt": "^3.5.4"
  }
}
